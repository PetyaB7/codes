n = int(input())
matrix = []

for y in range(n):
    numbers = input().split(", ")
    row = []
    for num in numbers:
        row.append(int(num))
    matrix.append(row)

sum_d = sum(matrix[i][i] for i in range(n))

print(sum_d)
