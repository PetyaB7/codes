n = int(input())
matrix = []

for y in range(n):
    numbers = input().split(", ")
    row = []
    for num in numbers:
        row.append(int(num))
    matrix.append(row)

sum_d = sum(matrix[i][j] for i in range(n) for j in range(i+1))
print(sum_d)
